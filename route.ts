import {NextResponse} from 'next/server';import {createClient} from '@supabase/supabase-js';import {callMusicProvider} from '@/lib/provider';import {finalizeWorkerOutput} from '@/lib/job-finalizer'
export async function POST(req:Request){const token=req.headers.get('authorization')?.replace(/^Bearer\s+/i,'');if(!token)return NextResponse.json({error:'Unauthorized'},{status:401});const sb=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,{global:{headers:{Authorization:`Bearer ${token}`}}});const {data:{user}}=await sb.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const body=await req.json();if(!Array.isArray(body.paths)||body.paths.length<1||body.paths.length>4)return NextResponse.json({error:'Upload 1–4 tracks'},{status:400});const {data:job,error}=await sb.from('sonora_jobs').insert({user_id:user.id,job_type:'mix',input_path:body.paths[0],metadata:{paths:body.paths,names:body.names||[]}}).select().single();if(error)return NextResponse.json({error:error.message},{status:500});try{const signed=[];for(const p of body.paths){const su=await sb.storage.from('sonora-audio').createSignedUrl(p,600);if(su.error)throw su.error;signed.push(su.data.signedUrl)}const provider=await callMusicProvider({operation:'mix',payload:{jobId:job.id,urls:signed}});await sb.from('sonora_jobs').update({status:'processing',metadata:{provider}}).eq('id',job.id);const outputUrl=await finalizeWorkerOutput(sb,job.id,user.id);return NextResponse.json({id:job.id,status:'completed',outputUrl,message:'Mixing completed.'})}catch(e){await sb.from('sonora_jobs').update({status:'failed',error_message:e instanceof Error?e.message:'Provider error'}).eq('id',job.id);return NextResponse.json({error:e instanceof Error?e.message:'Provider submission failed'},{status:502})}}
